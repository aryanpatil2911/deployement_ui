# This is a comment

name: Dummy Workflow  # Workflow name

on:
  workflow_dispatch:  # Manual trigger

jobs:
  demo:
    runs-on: ubuntu-latest  # Machine where job runs

    steps:
      # Step 1: Print message
      - name: Hello
        run: echo "Hello World"